{
  "name": "qr-attendance",
  "version": "1.0.0",
  "description": "QR-based attendance tracking system",
  "main": "api/index.ts",
  "scripts": {
    "build": "npm install && cd app/client && npm install && npm run build && cd ../.. && mkdir -p dist && cp -r app/client/dist/* dist/",
    "start": "node dist/server/index.js",
    "dev": "cd app/client && npm run dev"
  },
  "dependencies": {
    "@supabase/ssr": "0.6.1",
    "@supabase/supabase-js": "^2.39.3",
    "@vercel/node": "^3.0.22",
    "@yudiel/react-qr-scanner": "^2.2.1",
    "cookie": "^0.6.0"
  },
  "engines": {
    "node": "18.x"
  },
  "author": "",
  "license": "ISC"
}
