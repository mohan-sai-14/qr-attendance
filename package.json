{
  "name": "qr-attendance",
  "version": "0.1.0",
  "private": true,
  "description": "QR-based attendance tracking system",
  "main": "api/index.ts",
  "scripts": {
    "dev": "cd app/client && npm run dev",
    "build": "cd app/client && npm install && npm run build",
    "start": "cd app/client && npm run start",
    "lint": "cd app/client && npm run lint"
  },
  "dependencies": {
    "@supabase/ssr": "0.6.1",
    "@supabase/supabase-js": "^2.39.3",
    "@vercel/node": "^3.0.22",
    "cookie": "^0.6.0"
  },
  "engines": {
    "node": "18.x"
  },
  "author": "",
  "license": "ISC"
}
